/** @type {import('next').NextConfig} */
const nextConfig = {
   images: {
    domains: ['media.go-clothes.uz', 'placebear.com', 'source.unsplash.com', 'picsum.photos', 'via.placeholder.com'],
  },
 
};

export default nextConfig;
