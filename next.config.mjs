/** @type {import('next').NextConfig} */
const nextConfig = {
   images: {
    domains: ['media.go-clothes.uz', 'unsplash.com', 'source.unsplash.com', 'picsum.photos', 'via.placeholder.com', 'swiperjs.com'],
  },
 
};

export default nextConfig;
